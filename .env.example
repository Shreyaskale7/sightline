# --- SEC EDGAR (REQUIRED) ---
# The SEC requires a User-Agent identifying you (name + email). Requests without it get 403'd.
SEC_USER_AGENT="Your Name your@email.com"

# --- Storage / infra ---
QDRANT_URL="http://localhost:6333"
DATA_DIR="./data"

# --- LLM / VLM provider (fill in when you reach M1) ---
# Use a hosted vision-LLM API for answers early. Set a HARD budget alert on your account.
LLM_API_KEY=""
LLM_BASE_URL=""
LLM_MODEL="your-chat-model"
VLM_MODEL="your-vision-model"
# Cheap/small model for the router and the eval judge:
ROUTER_MODEL="your-small-model"

# --- Observability (optional; no-ops if left blank) ---
LANGFUSE_PUBLIC_KEY=""
LANGFUSE_SECRET_KEY=""
LANGFUSE_HOST="http://localhost:3000"

# --- Cost guardrail ---
MONTHLY_BUDGET_USD="10"
